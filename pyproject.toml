[project]
name = "nanowm"
version = "0.1.0"
description = "Pose-conditioned autoregressive diffusion transformer with spatial memory, at nano scale"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
